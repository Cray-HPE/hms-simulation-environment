name: Run Integration Tests
on: [push, pull_request, workflow_dispatch]
jobs:
  run_integration_test:

    name: Run Integration Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Install deps
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y make python3.8-venv
  
      - name: Run Integration Test
        shell: bash
        run: |
          make integration